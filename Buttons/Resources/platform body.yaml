body:
  type: kinematic

shapes:
  main:
    type: polygon
    half width: 0.5
    half height: 0.5

fixtures:
- shape: main
  friction: 1.0
  user data: Ground
  filter:
    category: Default
    no collide with: PassThrough
